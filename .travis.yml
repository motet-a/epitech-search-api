sudo: required
language: node_js
node_js:
  - "6"
services:
  - docker
script:
  - docker-compose -f docker-compose.test.yml -p epitech-search build
  - docker-compose -f docker-compose.test.yml -p epitech-search up --abort-on-container-exit
